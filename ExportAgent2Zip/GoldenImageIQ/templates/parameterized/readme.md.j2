# {{ project_name }}

{{ description }}

## Architecture

```
{{ project_name }}/
├── AIAgent/                    # Backend: FastAPI + AI Agent
│   ├── main.py                 # Entry point
│   ├── app/
│   │   ├── api.py              # FastAPI routes (SSE streaming)
│   │   ├── agent.py            # AI agent with function-calling tools
│   │   ├── core/
│   │   │   ├── config.py       # Configuration loader
│   │   │   └── models.py       # EvidenceRecord, FindingRecord, AssessmentResult
│   │   ├── collectors/         # Evidence collectors (auto-discovered)
{% for source in data_sources.keys() %}
│   │   │   ├── {{ source }}/
{% endfor %}
│   │   ├── {{ _project_snake }}_evaluators/  # Domain evaluators
│   │   └── reports/            # HTML, PDF, Excel, SARIF generators
│   ├── Dockerfile
│   └── requirements.txt
├── config/                     # Runtime configuration
├── webapp/                     # SPA frontend (vanilla JS + SSE)
├── Infra/                      # Azure deployment (deploy.ps1)
├── teams/                      # Teams app manifest
├── schemas/                    # JSON Schema definitions
└── output/                     # Generated reports
```

## Quick Start

```powershell
# 1. Deploy to Azure
cd Infra
.\deploy.ps1

# 2. Run locally
cd AIAgent
pip install -r requirements.txt
python main.py
```

## Domains

{% for domain in _all_domains %}
- **{{ domain }}**
{% endfor %}

## Frameworks

{% for fw in frameworks %}
- **{{ fw.name }}** ({{ fw.version }}) — {{ fw.controls | length }} controls across {{ fw.domains | join(', ') }}
{% endfor %}

## Reports

Supported formats: {{ reports | join(', ') }}

## Data Sources

{% for source, collectors in data_sources.items() %}
### {{ source | title }}
{% for coll in collectors %}
- `{{ coll.name }}` — evidence types: {{ coll.evidence_types | join(', ') }}
{% endfor %}
{% endfor %}

---
*Generated by [IQForge](https://github.com/your-org/iqforge)*
